import React from "react";
import { useGlobalContext } from "./context";

const Medallists = () => {
  return (
    <section>
      <h2>Medallists Component</h2>
    </section>
  );
};

export default Medallists;
